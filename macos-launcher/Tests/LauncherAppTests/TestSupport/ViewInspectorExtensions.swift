@testable import LauncherApp
import SwiftUI
import ViewInspector

// MARK: - ViewInspector Extensions

//
// Note: As of ViewInspector 0.10.0, explicit Inspectable conformance is no longer
// required. Views can be inspected directly without needing to extend them.
//
// This file is retained for any future custom ViewInspector extensions that may
// be needed, such as custom extraction methods for complex view hierarchies.
